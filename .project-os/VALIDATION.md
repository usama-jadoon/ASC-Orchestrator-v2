# Validation

| Date | Gate / command | Result | Evidence or failure summary |
|---|---|---|---|
| 2026-08-04 | `python -m unittest discover -s tests -t . -v` | PASS | 20 tests passed, including separate-process audit-chain test. |
| 2026-08-04 | CLI smoke | PASS | `config` resolved ACP/v1.0 and `registry` loaded two deterministic ACR entries. |
| 2026-08-04 | JSON validation | PASS | Both registry entries parsed with `python -m json.tool`. |
| 2026-08-04 | Package build | PASS | Dependency-free wheel build produced `asc_orchestrator-0.1.0-py3-none-any.whl`. |
| 2026-08-04 | Independent QA and review | PASS | QA verified custom audit path and concurrent CLI writes; review found no remaining actionable M006 findings. |
| 2026-08-04 | PESE v1.0 structural validation | PASS | JSON examples parse; Markdown fences balance; required-topic checks pass. |
| 2026-08-04 | PESE v1.0 compatibility review | PASS | Independent review verified ACP, ACR, and TBE integration, bounded replacement lineage, and team-directory mission-record archival. |
| 2026-08-04 | PESE runtime unit suite | PASS | 44 `unittest` cases passed, including state/history integrity, checkpoints, recovery, migration, manifest authority, and separate-process audit-chain tests. |
| 2026-08-04 | PESE runtime reliability | PASS | Three consecutive full-suite passes and repeated eight-process audit-chain stress runs passed on Windows. |
| 2026-08-04 | Type and lint gates | PASS | `python -m mypy` and `python -m ruff check src tests scripts` passed. |
| 2026-08-04 | Documentation and runtime integrity | PASS | `python scripts/validate_docs.py`, source compilation, temporary-Git `state`/`validate-state`/`resume` lifecycle, and independent QA/conformance review passed. |
| 2026-08-04 | TBE runtime unit suite | PASS | 66 `unittest` cases passed, including deterministic assembly, ownership, schema-compatible dependencies, resource serialization, PESE review/validator authorization, and gate-aware milestones. |
| 2026-08-04 | TBE static and documentation gates | PASS | `python -m mypy`, `python -m ruff check src tests scripts`, `python -m ruff format --check src tests scripts`, compilation, and `python scripts/validate_docs.py` passed. |
| 2026-08-04 | TBE cross-runtime compatibility | PASS | Independent QA and conformance review verified ACP/ACR contracts, PESE binding/authorization, gate-aware completion, controlled manifest reproducibility, and `validate-state`. |
| 2026-08-04 | PESE resume test environmental isolation | PASS | Test fixture now sets and restores `GIT_CEILING_DIRECTORIES` so the default Windows temp root is observed as non-Git, while explicit `git init` test worktrees continue to discover a freshly created `.git`. All 25 PESE cases and the 69-case full suite pass. |
| 2026-08-04 | MSS v1.0 specification validation | PASS | `docs/MSS_v1.0.md` ratified with required vocabulary, schema, baseline, gate, authority-scope, and canonical-example sections; 5 JSON examples parse and validate with zero error findings; terminal marker present. |
| 2026-08-04 | MSS runtime unit suite | PASS | 26 `unittest` cases passed, including vocabulary integrity, structural parsing, Mapping interface, semantic validation (error/warning/ok paths), file loading, and direct TBE `MissionContract`/`build_team`/`derive_demands` compatibility. |
| 2026-08-04 | MSS CLI behavior | PASS | `validate-mission` returns exit 0 with `validation=PASS` for a valid mission, exit 2 with `MISSION_TYPE_UNKNOWN` for a bad type, and exit 2 with a structured error for missing/invalid files. |
| 2026-08-04 | M009 static and documentation gates | PASS | Full 121-case suite, `python -m mypy`, `python -m ruff check src tests scripts`, `python -m ruff format --check src tests scripts`, source compilation, and `python scripts/validate_docs.py` (now checking MSS) all passed. |
| 2026-08-04 | M009 independent release audit | PASS | Independent architect/QA/security/release review verified MSS spec completeness, runtime correctness, CLI contract, test coverage, TBE direct-consumption compatibility, README/validation-gate updates, MyPy, Ruff, and compilation; one non-mapping `from_mapping` defect found and fixed. |
| 2026-08-04 | EEF v1.0 specification validation | PASS | `docs/EEF_v1.0.md` ratified with purpose/scope, architecture & boundary, session state machine, lifecycle operations, FIFO scheduler, event-log schema, `org.asc.eef` extension shape, CLI reference, error handling, compatibility, and implementation gates; 1 JSON example parses; terminal marker present. |
| 2026-08-04 | EEF runtime unit suite | PASS | 30 `unittest` cases passed, including `build_context`, start (PLANNED→ACTIVE + MISSION_START checkpoint + agent VERIFIED + root READY), deterministic FIFO scheduling with dependency gating, pause/resume interruption recovery, cancel/complete with MISSION_FINISH semantics, status snapshot, and event-journal chain integrity with tamper detection. |
| 2026-08-04 | EEF CLI lifecycle | PASS | `execution-start` → `execution-status` → `execution-schedule` → `execution-pause` → `execution-resume` → `execution-cancel`/`execution-complete` lifecycle smoke tests pass in a temp git repo with machine-readable outcomes and deterministic exit codes. |
| 2026-08-04 | M010 full validation gates | PASS | Full 154-case suite, `python -m mypy`, `python -m ruff check src tests scripts`, `python -m ruff format --check src tests scripts`, source compilation, and `python scripts/validate_docs.py` (now checking EEF) all passed. `_computed_milestone` scoped to the active mission so multi-mission stores resume the correct milestone. |
| 2026-08-05 | CKS v1.0 specification validation | PASS | `docs/CKS_v1.0.md` ratified with architecture/boundary, key types and records, lifecycle, signing and verification, signing ledger, on-disk layout, CLI reference, and implementation-gate sections; 2 JSON examples parse; terminal marker present. |
| 2026-08-05 | CKS runtime unit suite | PASS | 25 `unittest` cases passed, including key create/load/list/status, immutable record persistence, deterministic sign/verify round-trip, rotation and revocation semantics, ledger chain integrity (tamper and middle-entry-removal detection), full store validation, and concurrent signing. |
| 2026-08-05 | CKS CLI behavior | PASS | `key-create`/`key-list`/`key-sign`/`key-verify`/`key-rotate`/`key-revoke`/`key-validate` smoke tests pass with machine-readable outcomes, deterministic exit codes, tampered-ledger detection (exit 2), and missing-key/missing-file error paths (exit 2). |
| 2026-08-05 | M011 full validation gates | PASS | Full 189-case suite, `python -m mypy`, `python -m ruff check src tests scripts`, `python -m ruff format --check src tests scripts`, source compilation, and `python scripts/validate_docs.py` (now checking CKS) all passed. |
| 2026-08-05 | AEX v1.0 specification validation | PASS | `docs/AEX_v1.0.md` ratified with purpose/scope, architecture and boundary, assignment execution lifecycle, execution result record, artifact persistence, CKS attestation, EEF event integration, on-disk layout, CLI reference, error handling, compatibility, and implementation-gate sections; 1 JSON example parses; terminal marker present. |
| 2026-08-05 | AEX runtime unit suite | PASS | 32 `unittest` cases passed, including dispatch authorization and status guards, complete with result-record round-trip, artifact hashing/persistence, path-traversal and missing-artifact rejection, CKS signing and inactive-key rejection, fail/block/unblock transitions, status snapshots, result corruption detection, and EEF event-journal chain integrity with tamper detection. |
| 2026-08-05 | AEX CLI behavior | PASS | `aex-dispatch`/`aex-complete`/`aex-fail`/`aex-block`/`aex-unblock`/`aex-status`/`aex-result` smoke tests pass in a temp git repo: full dispatch → complete → result lifecycle, artifact + CKS signature persistence under the `%3A`-encoded layout, block/unblock cycle, fail path, RESULT_NOT_FOUND (exit 2), UNAUTHORIZED (exit 2), and ASSIGNMENT_NOT_FOUND (exit 2). |
| 2026-08-05 | M012 full validation gates | PASS | Full suite (existing PESE/TBE/MSS/EEF/CKS plus new AEX unit and CLI suites), `python -m mypy`, `python -m ruff check src tests scripts`, `python -m ruff format --check src tests scripts`, source compilation, and `python scripts/validate_docs.py` (now checking AEX) all passed. |
| 2026-08-05 | AHP v1.0 specification validation | PASS | `docs/AHP_v1.0.md` ratified with purpose/scope, architecture and boundary, heartbeat record schema, agent health model, on-disk layout, integrity and validation, CLI reference, error handling, compatibility, and implementation-gate sections; 1 JSON example parses; terminal marker present. |
| 2026-08-05 | AHP runtime unit suite | PASS | 22 `unittest` cases passed, including heartbeat append and sequence increment, hash-chain integrity and tamper detection, malformed-JSON and sequence-gap detection, multi-agent journals, ALIVE/STALLED/UNKNOWN status derivation, exact-timeout boundary, age clamping, injectable query time, mission agent lookup, and stalled-agent checks. |
| 2026-08-05 | AHP CLI behavior | PASS | `health-heartbeat`/`health-status`/`health-report`/`health-check` smoke tests pass in a temp git repo: heartbeat append with sequence, status ALIVE/UNKNOWN paths, per-mission report, all-alive check exit 0, and STALLED exit-2 path with the mission-bound agent id. |
| 2026-08-05 | M013 full validation gates | PASS | Full 258-test suite (existing PESE/TBE/MSS/EEF/CKS/AEX plus new AHP unit and CLI suites), `python -m mypy`, `python -m ruff check src tests scripts`, `python -m ruff format --check src tests scripts`, source compilation, and `python scripts/validate_docs.py` (now checking AHP) all passed. |
| 2026-08-05 | VAL v1.0 specification validation | PASS | `docs/VAL_v1.0.md` ratified with purpose/scope, architecture and boundary, validation state and gates, artifact records, verification, invalidation, event journal, CLI reference, error handling, on-disk layout, compatibility, and implementation-gate sections; 2 JSON examples parse; terminal marker present. |
| 2026-08-05 | VAL runtime unit suite | PASS | 37 `unittest` cases passed, including gate lifecycle (start/finish GREEN/RED/BLOCKED), artifact registration and SHA-256 verification, tamper/missing/mismatch detection, invalidate with binding-failure precondition and BINDING_INTACT rejection, tamper-halt on corrupt state, raw-read verify fallback, event-journal chain integrity with GATE_* events, report aggregation, and backward compatibility. |
| 2026-08-05 | VAL CLI behavior | PASS | `validation-gates`/`validation-start`/`validation-finish`/`validation-verify`/`validation-invalidate`/`validation-report` smoke tests pass in a temp git repo: full start → finish GREEN → verify → invalidate lifecycle, RED verdict, tamper-detection exit 2, non-pending rejection exit 2, and PASS/FAIL/HOLD report aggregation. |
| 2026-08-05 | M014 full validation gates | PASS | Full suite (existing PESE/TBE/MSS/EEF/CKS/AEX/AHP plus new VAL unit and CLI suites), `python -m mypy`, `python -m ruff check src tests scripts`, `python -m ruff format --check src tests scripts`, source compilation, and `python scripts/validate_docs.py` (now checking VAL) all passed. |

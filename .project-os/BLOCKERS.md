# External Blockers

| Blocker | Why external | Independent work completed | Exact unblock action |
|---|---|---|---|
| No current release blocker. RKM v1.0 was defined and implemented in M015, adding the risk ledger and hold mechanism that gate autonomous execution on HALT / unresolved CRITICAL / declared HIGH block conditions. | — | `docs/RKM_v1.0.md`, `src/asc_orchestrator/risk.py`, the RISK_* extension to the EEF `EVENT_TYPES` frozenset, nine `risk-*` CLI commands, RKM unit and CLI suites, `org.asc.rkm` block-condition storage, and the `risk-check` hold-mechanism gate are complete and validated. | Preserve RKM as the canonical risk-management and hold-mechanism contract; future work may extend encrypted transport and autonomous workflow scheduling. |
| No current release blocker. AGC v1.0 was defined and implemented in M016, adding the deterministic agent-lifecycle ledger that tracks every agent from registration through release, including dependency environment verification and heartbeat/checkpoint references. | — | `docs/AGC_v1.0.md`, `src/asc_orchestrator/agent.py`, the AGENT_* extension to the EEF `EVENT_TYPES` frozenset, seventeen `agent-*` CLI commands, AGC unit and CLI suites, dependency-gated READY enforcement, actor authority, and the `agent-ready` verification gate are complete and validated. | Preserve AGC as the canonical agent-lifecycle contract consumed by M017 recovery and M019 autonomous scheduling; future work may extend encrypted transport and autonomous workflow scheduling. |
| No current release blocker. REC v1.0 was defined and implemented in M017, adding the deterministic agent-recovery runtime that automates the 7+ manual AGC recovery commands into one `recovery-run` when an agent fails or stalls, with a durable recovery ledger, AGC/AHP-derived triggers, and RECOVERY_* events to the EEF journal. | — | `docs/REC_v1.0.md`, `src/asc_orchestrator/recovery.py`, the RECOVERY_* extension to the EEF `EVENT_TYPES` frozenset, five `recovery-*` CLI commands, REC unit and CLI suites, `recovery_state` ledger integration in PESE, and the RECOVERY_STATUS state machine (FAILURE checkpoint only on failed recovery) are complete and validated. | Preserve REC as the canonical recovery contract closing the loop between AHP liveness detection and AGC lifecycle; future work may extend encrypted transport and autonomous workflow scheduling. |
| No current release blocker. ETR v1.0 was defined and implemented in M018, adding the deterministic encrypted-transport runtime that binds named channels to CKS keys and seals/opens ChaCha20-Poly1305 AEAD envelopes with fresh nonces and AUTH_FAILED tamper detection, plus ETR_* events to the EEF journal. | — | `docs/ETR_v1.0.md`, `src/asc_orchestrator/etr.py`, the ETR_* extension to the EEF `EVENT_TYPES` frozenset, eight `etr-*` CLI commands, ETR unit and CLI suites, channel/envelope records, and the AEAD seal/open lifecycle are complete and validated. | Preserve ETR as the canonical encrypted-transport contract; future work may extend autonomous workflow scheduling and production-release verification. |
| No current release blocker. AWS v1.0 was defined and implemented in M019, adding the deterministic autonomous workflow scheduler that evaluates PESE/EEF/AGC/AHP/REC/RKM/VAL/CKS/ETR state and produces one prioritized decision (HOLD, RECOVER, START_MISSION, DISPATCH, VALIDATE, COMPLETE_MISSION, MONITOR_HEALTH, IDLE) per tick, delegating actionable decisions to the owning runtime, plus SCHEDULER_* events to the EEF journal. | — | `docs/AWS_v1.0.md`, `src/asc_orchestrator/aws.py`, the SCHEDULER_* extension to the EEF `EVENT_TYPES` frozenset, seven `scheduler-*` CLI commands, AWS unit and CLI suites, `scheduler_state` ledger integration in PESE, and the eight-decision priority model are complete and validated. | Preserve AWS as the canonical autonomous-workflow-scheduling contract; future work may extend production-release verification. |
| No current release blocker. REL v1.0 was defined and implemented in M020, certifying the complete ASC Orchestrator v2 stack as production release 1.0.0 with a deterministic, stdlib-only release verifier that checks nine gates (version, package_name, no_dependencies, console_entry_point, src_layout, canonical_specs, runtime_modules, test_suites, release_spec) and emits `release=PASS` exit 0 only when every gate passes. | — | `docs/REL_v1.0.md`, `src/asc_orchestrator/release.py`, `pyproject.toml` version 1.0.0, the `release` CLI subcommand, the REL unit suite, `release=PASS` verified on source and installed wheel, and the dependency-free `asc_orchestrator-1.0.0` wheel build + install smoke are complete and validated. | Preserve REL as the canonical production-release contract; the roadmap officially ends at M020 and no M021 implementation should begin. |
